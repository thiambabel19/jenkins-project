pipeline {
   agent any
   stages {
     stage(‘Build’) {
     steps {
       sh "/opt/java/openjdk/bin/mvn clean package"
     }
   }
   stage(‘Test’) {
     steps {
         sh "/opt/java/openjdk/bin/mvn test"
       }
     }
   }
}
